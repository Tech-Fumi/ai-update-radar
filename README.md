# AI Update Radar

**AIの本質的な更新を検知・評価・実験するための運用リポジトリ**

> 全部追う人ほど疲弊し、作る人ほど勝つ。
> このリポジトリは「使える進化だけが自然に流れ込む構造」を実現する。

---

## 思想（Philosophy）

### なぜこのリポジトリが必要か

AI界隈の情報量は人間の処理限界を超えている：
- 新モデル（毎週〜毎日）
- 新ツール（Agents / Browser / IDE / MCP / RPA）
- 新フレームワーク
- 新API・新料金体系

**全部追うと**：情報疲労 → 判断力低下 → 実装が止まる → 収益化が遅れる

### このリポジトリの立ち位置

```
❌ 新しいAIツール集
❌ ニュースまとめ
❌ 技術デモ

✅ AI進化を"使える変化"に変換するフィルター
✅ 全プロジェクト共通の判断レイヤー
```

---

## 判断基準（Evaluation Criteria）

キャッチアップ対象を3層に分離する：

### Layer 1: 無視（90%）

- 「◯◯がすごい！」系SNS
- 表面的な比較レビュー
- UIが変わっただけのツール
- "〇〇をAIでやってみた"系

**→ 見ない・読まない・議論しない**

### Layer 2: 検知のみ（自動）

以下の変化だけを検知する：

| カテゴリ | 例 |
|----------|-----|
| **能力変化** | Browser常駐、Memory永続化、Agent間通信、OS操作、IDE常駐 |
| **制限解除** | コンテキスト長拡張、レート制限緩和、新機能アンロック |
| **価格変化** | 料金改定、新プラン、無料枠変更 |

**→ 詳細は読まない。「変化が起きたか」だけ検知**

### Layer 3: 深掘り（超少数）

以下の条件を**すべて**満たす場合のみ実験：

1. 今の自分の仕組みに
2. そのまま差し込める
3. 時間 or コスト or 人力を削減する

**→ 30〜90分上限で実験。ダメなら捨てて記録だけ残す**

---

## 公開/非公開の境界線（Transparency Policy）

### なぜ公開するのか

1. **信用の構築**: 思想・設計を先に言語化した人が標準になる
2. **説明コスト削減**: README が営業資料になる
3. **エコシステム形成**: 質問・改善案・実装者が集まる

### 公開するもの

| 項目 | 状態 |
|------|:----:|
| 判断思想 | ✅ 公開 |
| 評価カテゴリ | ✅ 公開 |
| 判断フロー | ✅ 公開 |
| 週次要約フォーマット | ✅ 公開 |
| やらなかった決定 | ✅ 公開 |
| 失敗パターン（一般化） | ✅ 公開 |

### 公開しないもの

| 項目 | 状態 |
|------|:----:|
| 実データ | ❌ 非公開 |
| 数値ログ | ❌ 非公開 |
| 顧客/案件情報 | ❌ 非公開 |
| 本番設定 | ❌ 非公開 |
| 運用閾値の実値 | ❌ 非公開 |

### 公開しても競合に負けない理由

```
設計はコピーできる。
運用の履歴はコピーできない。
信頼は履歴でしか作れない。
```

- 判断ログの蓄積
- 捨てた理由の記録
- 現場で調整された閾値
- 実運用で壊れた履歴
- 人間の介入判断の癖

**→ これらは「運用し続けた側」にしか生まれない**

---

## ディレクトリ構造

```
ai-update-radar/
├── sources/           # 監視対象定義（公式ブログ、GitHub releases、価格ページ等）
├── collectors/        # 収集スクリプト
├── evaluators/        # 影響判定ロジック（能力変化/制限解除/価格変化）
├── experiments/       # PoC置き場（隔離環境）
├── docs/
│   └── weekly/        # 週次サマリ
├── exports/           # 他リポジトリへ渡す成果物
└── .private/          # 非公開データ（.gitignore対象）
```

---

## 運用フロー

### 週次サイクル

```
月曜: 監視対象から変化を検知
      ↓
火〜木: Layer 2 → Layer 3 の選別
      ↓
金曜: 実験（30-90分上限）
      ↓
週末: 週次サマリ作成 → exports/
```

### 公開用ダイジェスト生成

exports のデータから、ブログ/Note/X 向けの公開用コンテンツを自動生成：

```bash
# 最新週の全フォーマットを生成
python3 scripts/generate-public-digest.py

# 特定週を指定
python3 scripts/generate-public-digest.py --week 2025-W51

# X向けのみ生成
python3 scripts/generate-public-digest.py --format x

# プレビュー（ファイル出力なし）
python3 scripts/generate-public-digest.py --dry-run
```

**出力先**: `docs/weekly/public-{week}-{format}.md`

| フォーマット | 用途 | 特徴 |
|-------------|------|------|
| `blog` | ブログ記事 | フル構造、URL付き |
| `note` | Note記事 | ブログと同等 |
| `x` | X (Twitter) | 280字以内、ハッシュタグ付き |

### 週次自動公開（全自動化）

完全自動化スクリプトで、判定・通知・投稿準備を一括実行：

```bash
# 通常実行（Discord通知付き）
python3 scripts/weekly-auto-publish.py

# プレビュー（実行せず確認のみ）
python3 scripts/weekly-auto-publish.py --dry-run

# 強制レビューモード
python3 scripts/weekly-auto-publish.py --force-review
```

**自動判定ロジック：**

| 条件 | 動作 |
|------|------|
| Layer3 = 0 かつ 重要アラートなし | 🟢 自動投稿OK（Discord通知） |
| Layer3 > 0 または security/breaking あり | 🟡 要確認（drafts/ に保存 + Discord通知） |

**cron 設定例（毎週日曜 20:00）：**

```bash
# crontab -e で追加
0 20 * * 0 cd /home/fumi/ai-update-radar && python3 scripts/weekly-auto-publish.py >> logs/auto-publish.log 2>&1
```

**環境変数：**

```bash
export DISCORD_ALERT_WEBHOOK_URL="https://discord.com/api/webhooks/..."
```

### マーケティング機能

競合分析・トレンド検知・SNS投稿候補生成・効果測定連携：

```bash
# トレンド検知 + SNS投稿候補生成
python3 -m collectors.cli marketing

# 効果測定サマリも表示
python3 -m collectors.cli marketing --analytics

# トレンド検知のみ
python3 -m collectors.cli marketing --no-content
```

**機能一覧：**

| 機能 | 説明 | 出力先 |
|------|------|--------|
| 競合分析 | WebSearchで競合情報を収集 | `.private/marketing/competitors/` |
| トレンド検知 | キーワード頻度の変化を検出 | `.private/marketing/trends/` |
| 効果測定連携 | 投稿パフォーマンス追跡 | `.private/marketing/analytics/` |
| SNS投稿候補 | トレンド・ダイジェストから自動生成 | `.private/marketing/content/` |

**監視対象設定：** `sources/competitors.yaml`

### 判断ログの義務化

すべての判断に以下を記録：

```markdown
## [YYYY-MM-DD] {対象名}

### 検知内容
- カテゴリ: 能力変化 / 制限解除 / 価格変化
- 概要:

### 判断
- 結果: 深掘り / 様子見 / 無視
- 理由:

### 実験結果（深掘りの場合）
- 所要時間:
- 結論: 採用 / 不採用
- 不採用理由:
```

---

## 採用条件（Adopt Rule）

Layer 3 の実験を経て「採用」とするための条件：

### 必須条件（すべて満たす）

| # | 条件 | 判定基準 |
|:-:|------|----------|
| 1 | **動作した** | 30〜90分以内にエラーなく動く状態になった |
| 2 | **既存に差し込める** | 既存システムの変更が3ファイル以内で収まる |
| 3 | **効果が測定可能** | 時間・コスト・品質のいずれかで効果を数値化できる |
| 4 | **リスクが許容内** | 壊れても30分以内にロールバック可能 |

### 判定フロー

```
実験完了
    ↓
4条件すべて✅ → exports/adopted-YYYY-WXX.yaml に記録
    ↓           → 本番導入をスケジュール

1つでも❌ → experiments/ にログを残して終了
           → 再検討条件があれば recorded_at に記載
```

### 採用後のアクション

| 採用レベル | アクション |
|------------|------------|
| **即時導入** | 今週中に本番環境に反映 |
| **計画導入** | 次スプリントで導入スケジュール |
| **保留** | 条件付きで再検討（例: 料金改定後、安定版リリース後） |

### 失敗パターン（不採用になりやすいケース）

- セットアップに1時間以上かかる
- 既存の認証・権限と衝突する
- ドキュメントが英語のみで不完全
- ベータ/プレビュー版で本番利用非推奨
- 依存ライブラリが不安定

---

## 価値観（Values）

すべての判断の拠り所：

| 優先 | 価値観 | 例 |
|:----:|--------|-----|
| 1 | **安全 > 速さ** | 迷ったら安全な方を選ぶ |
| 2 | **小さく試す > 大きく賭ける** | 確信がないならスモールスタート |
| 3 | **聞く > 推測** | 不確実なら人間に聞く |
| 4 | **記録 > 記憶** | 重要なことはファイルに残す |
| 5 | **自動化 > 手動** | 繰り返すなら自動化 |
| 6 | **削除 > 放置** | 使わないものは消す |

---

## 将来の展開

### Phase 1（現在）
- 思想の言語化
- 判断軸の確立
- 週次運用の開始

### Phase 2（慣れてきたら）
- 抽象化した事例の公開
- 失敗パターンの一般化
- 自動検知の強化

### Phase 3（収益化）
- 本番運用は非公開維持
- 個別支援・コンサル
- 有償レビュー

---

## 関連リポジトリ

- [infra-automation](https://github.com/fumiya007/infra-automation) - DevOps / MCP サーバー群
- decision-ledger - 判断ログ管理
- snippet-collector - 成功パターン蓄積

---

## License

Currently unlicensed. Will be determined based on future direction.
