# 収集の実行方法

AI 更新情報を収集する手順。

## 手順

1. `sources/` で監視対象を確認
2. 収集スクリプトを実行

```bash
python collectors/run.py
```

3. 結果を確認

## 監視対象の追加

`sources/` に新しいソースを追加する場合:

1. `sources/` に定義ファイルを作成
2. `collectors/` に対応スクリプトを追加

## トラブルシューティング

- API エラー: 認証情報を確認
- タイムアウト: リトライ設定を確認
