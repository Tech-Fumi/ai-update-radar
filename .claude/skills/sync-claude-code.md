# /sync-claude-code

Claude Code の新しいリリースを検知し、ドキュメントを更新するスキル。

## トリガー

- 「Claude Code 更新確認して」
- 「リリース同期して」
- `/sync-claude-code`

## 前提条件

```bash
# venv が必要（初回のみ）
cd /home/fumi/ai-update-radar/collectors/claude_code
python3 -m venv .venv
.venv/bin/pip install feedparser requests
```

## 手順

### 1. 現在の状態確認

```bash
# インストール済みバージョン（native installer）
claude --version

# 状態ファイル（最後に検知したバージョン）
cat /home/fumi/ai-update-radar/collectors/claude_code/.last_release_state.json

# releases.json の最新
jq -r '.releases[0].version' /home/fumi/ai-update-radar/frontend/public/data/releases.json
```

### 2. GitHub から最新リリース取得

```bash
curl -s "https://api.github.com/repos/anthropics/claude-code/releases?per_page=20" | \
  jq -r '.[] | "\(.tag_name) - \(.published_at[:10])"' | head -10
```

### 3. 欠けているリリースがあれば releases.json を更新

```python
# /tmp/update_releases.py として実行
import json
import re
import requests

releases_api = requests.get(
    "https://api.github.com/repos/anthropics/claude-code/releases?per_page=50"
).json()

with open("/home/fumi/ai-update-radar/frontend/public/data/releases.json") as f:
    data = json.load(f)

existing = {r["version"] for r in data["releases"]}

for r in releases_api:
    v = r["tag_name"]
    if v not in existing:
        body = r.get("body", "") or ""
        highlights = [line[2:].strip() for line in body.split("\n")
                      if line.strip().startswith(("* ", "- "))][:5]
        if not highlights:
            highlights = ["Various improvements and bug fixes"]

        data["releases"].append({
            "version": v,
            "date": r["published_at"][:10],
            "link": r["html_url"],
            "highlights_en": highlights,
            "highlights_ja": [],
            "meanings": []
        })
        print(f"Added: {v}")

# バージョン順ソート
def ver_key(x):
    m = re.match(r'v?(\d+)\.(\d+)\.(\d+)', x["version"])
    return (int(m.group(1)), int(m.group(2)), int(m.group(3))) if m else (0,0,0)

data["releases"].sort(key=ver_key, reverse=True)

with open("/home/fumi/ai-update-radar/frontend/public/data/releases.json", "w") as f:
    json.dump(data, f, indent=2, ensure_ascii=False)
```

### 4. ドキュメント作成

欠けているバージョンが複数ある場合、まとめドキュメントを作成:

**ファイル名:** `docs/claude-code/updates-{old}-to-{new}.md`

**構成:**
```markdown
# Claude Code 新機能まとめ（{old} → {new}）

作成日: YYYY-MM-DD

## 重要度: 高
（新機能、仕様変更）

## 重要度: 中
（カスタマイズ、開発者向け、UI/UX）

## 重要度: 低
（バグ修正）

## バージョン履歴
（各バージョンの一覧表）

## アップグレード推奨
```

### 5. 状態ファイル更新

```bash
cd /home/fumi/ai-update-radar/collectors/claude_code
.venv/bin/python monitor.py
```

### 6. ルール影響判定

リリース内容を確認し、ルール更新が必要かを判定する。

**ルール更新が必要なパターン:**

| 変更の種類 | ルール更新 | 例 |
|-----------|:----------:|-----|
| インストール方法の変更 | ✅ 必要 | npm → native installer |
| 新コマンド追加 | ✅ 必要 | `claude install`, `--from-pr` |
| 既存動作の変更 | ✅ 必要 | デフォルト値変更、廃止 |
| 新機能（オプション） | ⚠️ 検討 | 便利機能、設定項目 |
| バグ修正 | ❌ 不要 | - |
| パフォーマンス改善 | ❌ 不要 | - |

**判定フロー:**

```
1. リリースノートの各項目を確認
2. 「ルール更新が必要なパターン」に該当するか判定
3. 該当する場合:
   a. 関連ルールを grep で検索
   b. 外部AIに相談（send_consultation）
   c. ルール更新 or 新規作成
4. 該当しない場合: スキップ
```

**関連ルールの検索例:**

```bash
# インストール関連
grep -r "npm\|install\|claude-code" ~/.claude/rules/

# コマンド関連
grep -r "claude " ~/.claude/rules/

# セッション関連
grep -r "session\|--from-pr" ~/.claude/rules/
```

**事例（2026-02-01）:**
- 変更: npm → native installer への移行
- 影響: `hash-cache.md` にトリガー追加、`sync-claude-code.md` の手順更新
- 新規: `context-recovery.md` に `--from-pr` セクション追加

### 6.1 ルール反映確認

ルール影響判定で「必要」と判定された項目が、実際にルールに反映されているか確認する。

**確認フロー:**

```
1. 判定で「必要」となった項目をリストアップ
2. 各項目について grep で既存ルールを検索
3. 反映状況を判定:
   - ✅ 反映済み: ルールに記載あり
   - ⚠️ 未反映: ルールに記載なし → 追加が必要
   - 🆕 新規作成済み: 新しいルールファイルを作成した
```

**確認コマンド:**

```bash
# 新機能・コマンドがルールに記載されているか
grep -r "<新機能キーワード>" ~/.claude/rules/ --include="*.md"

# 例: --from-pr の確認
grep -r "from-pr" ~/.claude/rules/
# → context-recovery.md に記載があれば ✅

# 例: native installer の確認
grep -r "claude install" ~/.claude/rules/
# → hash-cache.md に記載があれば ✅
```

**未反映時のアクション:**

| 状況 | アクション |
|------|-----------|
| 既存ルールに追記すべき | Edit で該当ルールを更新 |
| 新規ルールが必要 | Write で新規作成 |
| 判断が難しい | 外部AIに相談（send_consultation） |

**確認結果テンプレート:**

```
ルール反映確認:
- `--from-pr`: ✅ context-recovery.md に追加済み
- `claude install`: ✅ hash-cache.md に追加済み
- `Task delete`: ✅ todo-write.md に追加済み
- `spinnerVerbs`: ❌ 不要（オプション設定のため）
```

### 7. AIOS 活用判定

リリース内容が AIOS（AI 自動化インフラ）に活用できるか判定する。

**AIOS 活用可能なパターン:**

| 変更の種類 | 活用度 | 活用例 |
|-----------|:------:|--------|
| 新 API / MCP 機能 | ★★★ | MCP サーバー連携強化 |
| フック / イベント追加 | ★★★ | 自動トリガー、監視 |
| CLI オプション追加 | ★★☆ | `--from-pr`, `--resume` |
| バッチ / 並列処理改善 | ★★☆ | 大量タスク処理 |
| セッション管理強化 | ★★☆ | 復帰、履歴管理 |
| UI / エディタ機能 | ★☆☆ | 人間向け（自動化向きでない） |
| バグ修正 | ☆☆☆ | 活用対象外 |

**判定フロー:**

```
1. リリースノートの各項目を確認
2. 「AIOS 活用可能なパターン」に該当するか判定
3. ★★以上の場合:
   a. 活用アイデアをメモ
   b. AIOS 全体への統合を検討（下記参照）
   c. 必要なら実験（experiments/）
4. ★以下: スキップ
```

**AIOS 構成と活用先:**

| レイヤー | リポジトリ | 活用対象 |
|----------|-----------|----------|
| 基盤 | infra-automation | MCP サーバー、フック、自動化スクリプト |
| ルール | ~/.claude/rules/ | グローバルルール、ワークフロー |
| 監視 | ai-update-radar | 更新検知、評価 |
| 運用 | global-market-regime 等 | プロジェクト固有の自動化 |

**活用検討時の確認:**

```bash
# AIOS 全体での関連確認
grep -r "<新機能キーワード>" ~/infra-automation/ ~/.claude/rules/ ~/ai-update-radar/

# MCP サーバー一覧
ls ~/infra-automation/mcp-servers/

# グローバルルール
ls ~/.claude/rules/

# フック
ls ~/.claude/hooks/
```

**事例:**
| バージョン | 機能 | 活用先 |
|-----------|------|--------|
| v2.1.20 | `--from-pr` | ~/.claude/rules/（context-recovery.md） |
| v2.1.16 | Task* ツール | ~/.claude/rules/（todo-write.md） |
| v2.1.x | native installer | ~/.claude/rules/（hash-cache.md）, スキル |

### 7.1 統合検討

★★以上の機能について、AIOS への統合を検討する。

**検討フロー:**

```
1. AI が統合案を作成
   - 統合先（MCP / フック / ルール / スクリプト）
   - 統合内容（何を追加するか）
   - 優先度（今すぐ / 中 / 後で / 不要）
   - 理由

2. 外部AIにチェック（send_consultation）
   - 統合案の妥当性
   - 見落としがないか
   - 優先度は適切か

3. 結果に応じて実行
   - 一致 or 修正採用 → 次のステップへ
   - 不一致 → 再検討

4. タスクを snippet-collector に追加
   - 優先度「不要」以外はタスク登録
   - タグ: task, todo, <統合先>, <リポジトリ>
   - 後から検索可能に
```

**統合案テンプレート:**

```
機能: --from-pr
活用度: ★★★
統合先: session-manager MCP
統合内容: list_sessions に PR 番号フィルタを追加
優先度: 後で（既にルールで対応済み、MCP 統合は追加価値）
理由: claude --from-pr で直接復帰できるため、MCP 統合の優先度は低い
```

**外部AI相談テンプレート:**

```python
mcp__task-receiver__send_consultation(
    situation="Claude Code 新機能の AIOS 統合検討",
    options=["A: 統合する", "B: 後で検討", "C: 統合不要"],
    question="以下の統合案は妥当か？\n\n" + 統合案,
    consultation_type="strategy"
)
```

**Issue の保存先（必須判定）:**

Issue は**実装先のリポジトリ**に作成する。ai-update-radar は監視リポジトリであり、実装タスクの置き場ではない。

| 統合先 | Issue 保存先 |
|--------|-------------|
| MCP サーバー（model-selector 等） | infra-automation |
| フック / ルール / スクリプト | infra-automation |
| フロントエンド（ai-update-radar） | ai-update-radar |
| 他プロジェクト固有 | 該当プロジェクト |

```
❌ 「sync-claude-code で検出したから ai-update-radar に Issue」
✅ 「実装先が infra-automation だから infra-automation に Issue」
```

**統合作業（一致した場合）:**

| 統合先 | 作業内容 |
|--------|----------|
| MCP サーバー | 機能追加 → テスト → PR |
| フック | スクリプト追加 → settings.json 登録 |
| ルール | Edit で追記 or Write で新規作成 |
| スクリプト | scripts/ に追加 |

**タスク追加（優先度「不要」以外）:**

```python
mcp__snippet-collector__save_snippet(
    name="task-<統合先>-<機能名>",
    content="""# <タスクタイトル>

## 背景
<Claude Code のリリースノートから>

## タスク
<何を実装するか>

## 実装案
1. ...
2. ...

## 優先度
<今すぐ / 中 / 後で>

## 参照
- Claude Code vX.X.X リリースノート
- GPT 相談: CONS-XXXXXXXX-XXXXXX-XXXX
""",
    snippet_type="other",
    tags=["task", "todo", "<統合先>", "<リポジトリ>"]
)
```

**タスク検索:**

```python
mcp__snippet-collector__search_snippets(tags=["task", "infra-automation"])
```

## チェックリスト

```
□ releases.json に新バージョン追加
□ docs/claude-code/ にドキュメント作成
□ .last_release_state.json 更新
□ ルール影響判定を実施
□ 必要ならルール更新（外部AI確認必須）
□ ルール反映確認（grep で既存ルールに記載があるか）
□ AIOS 活用判定を実施
□ ★★以上なら統合案を作成
□ 統合案を外部AIにチェック（send_consultation）
□ 優先度「不要」以外はタスクを snippet-collector に追加
□ git commit & push（任意）
```

## 出力例

```
v2.1.12 → v2.1.29 の更新を検知
- releases.json: 12件追加
- ドキュメント: docs/claude-code/updates-2.1.12-to-2.1.29.md 作成
- 状態ファイル: v2.1.29 に更新
```
